<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu"></activity>
        <activity android:name=".work20201009.Question_04">
            <intent-filter>
                <action android:name="activity4" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".work20201009.Question_03">
            <intent-filter>
                <action android:name="activity3" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity> <!-- .MainActivity2为目标页面的Java文件名称，前面有加点 -->
        <activity android:name=".work20201009.Question_02">
            <intent-filter>

                <!-- 此处的activity2为前面声明的名字 -->
                <action android:name="activity2" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".work20201009.Question_01">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>